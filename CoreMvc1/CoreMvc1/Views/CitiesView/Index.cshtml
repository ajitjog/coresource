@using Microsoft.Extensions.Configuration;
@inject IConfiguration configuration;

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<script>
    $(document).ready(function () {
        alert('in');

        $("#btn").click(function () {

        $.ajax({
            url: '@configuration.GetSection("externalservices")["apiurl2"]',
            type: "GET",
            success: function (data) {
                console.log(data);
                var str="";
                data.forEach(function (ct) {
                    str += "<li>";
                    str += ct.cityName + "  " + ct.cityDensity
                });
                $("#cts").html(str);
            }
        })
    });

        });


        $.ajax({
            url: '@configuration.GetSection("externalservices")["apiurl"]',
            type: "GET",
            success: function (data) {
                console.log(data);
                var str="";
                data.forEach(function (ct) {
                    str += "<li>";
                    str += ct.cityName + "  " + ct.cityDensity
                });
                $("#cts").html(str);
            }
        })
    });

</script>
<ul id="cts">


</ul>
<hr/>
<input id="btn" type="button" value="Click Me"/>

